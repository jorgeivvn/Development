<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Jorge's Homepage</title>
    <link href="https://fonts.googleapis.com/css?family=Asap" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="/styles.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">

    <meta name="viewport" content="width=device-width, initial-scale=1">
  </head>
  <body>
    <body>
<h1 class="round"> Welcome Jorge! </h1>

<!-- Navigation Bar  -->
<ul class="nav nav-pills nav-fill">
  <li class="nav-item">
    <a class="nav-link" href="/">Home</a>
  </li>
  <li class="nav-item">
    <a class="nav-link" href="https://cloud.oracle.com/home">Oracle Cloud</a>
  </li>
  <li class="nav-item">
    <a class="nav-link" href="https://www.espn.com">ESPN</a>
  </li>
  <li class="nav-item">
    <a class="nav-link" href="https://travel-blog-7160d.firebaseapp.com/">Nomad Travel Blog</a>
  </li>
</ul>

    <div class="container">
      <h2>What are your tasks for today, Jorge?</h2>
      <form action ="/addtask" method="POST">
        <input type="text" name="newtask" placeholder="Add all tasks for today...">
        <button class="btn btn-primary">Add</button>
      <h2>Tasks For Today:</h2>
        <% for(var i=0; i < tasks.length; i++){ %>
          <ol><input type="checkbox" name="check" value="<%= tasks[i] %>" /> <%= tasks[i] %> </ol>
          <% } %>
      <button formaction="/deletetask" type="submit" class="btn btn-primary"> Completed </button>
    </form>
    <h2>Finished Tasks:</h2>
    <% for(var i = 0; i < complete.length; i++){ %>
      <ol><input type="checkbox" checked><%= complete[i] %> </ol>
    <% } %>
    </div>

    <div class="container">
      <h2>Weather</h2>

        <form action="/getweather" method="post">
          <input name="city" type="text" class="ghost-input" placeholder="Current Location" required>
          <input type="submit" class="btn btn-primary" value="Get Weather">
        </form>
        <% if(weather !== null){ %>
        <h2><%= weather %></h2>
      <% } %>
      <% if(error !== null){ %>
      <h2><%= error %></h2>
    <% } %>

    </div>
  </body>
</html>
